const Wallet = require("./Wallet");
const Transaction = require("./Transaction");
const User = require("./User");  // Add this line to import User

module.exports = { Wallet, Transaction, User };  // Export User as well
